import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    header: {
        fontWeight: 'bold',
        fontSize: '1.1rem',
        color: '#555',
    }
});

export default useStyles;